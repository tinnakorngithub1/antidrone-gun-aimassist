#!/bin/bash
# ============================================================
#  โหมดทดสอบ — โดรนเสมือน (virtual target)
#  แขนจริง + กล้องจริง แต่ฉีดโดรนปลอมลงเฟรม เพื่อทดสอบ LOCK/fire
#  โดยไม่ต้องบินโดรนจริง — ปลอดภัยสำหรับซ้อม
#  ปุ่ม: L=เข้า LOCK, K=ล็อกเป้า, ลูกศร=บังคับโดรน, v=สลับ pattern
# ============================================================
cd "$(dirname "$0")"
source ~/antidrone_v2/bin/activate 2>/dev/null || { echo "❌ ไม่พบ venv ~/antidrone_v2"; exit 1; }
echo "========================================="
echo "  โหมด: ทดสอบ (โดรนเสมือน ENABLED)"
echo "  ⚠️  อย่าใช้โหมดนี้ตอนภารกิจจริง"
echo "========================================="
ANTIDRONE_SIM=1 python3 22_gun_aim_assist_vector.py "$@"
