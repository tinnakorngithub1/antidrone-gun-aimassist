#!/bin/bash
# ============================================================
#  ใช้งานจริง (ภาคสนาม) — ไม่มีโดรนเสมือน
#  แขนจริง (GRBL) + กล้องจริง + YOLO + รับ cue จาก cam8
# ============================================================
cd "$(dirname "$0")"
source ~/antidrone_v2/bin/activate 2>/dev/null || { echo "❌ ไม่พบ venv ~/antidrone_v2"; exit 1; }
echo "===================================="
echo "  โหมด: ใช้งานจริง (โดรนเสมือน OFF)"
echo "===================================="
ANTIDRONE_SIM=0 python3 22_gun_aim_assist_vector.py "$@"
